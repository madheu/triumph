# Triumph · 部署说明（免费上线，拿真实 URL）

> 用途：原型本地跑通后，免费部署到线上，拿到公开 URL 填进 Reddit 帖和问卷
> 前提：`Landing Page.html` + `Diagnostic Demo.html` 在同一文件夹，互链已通（相对路径）

## 方案 A：Netlify Drop（最快，2 分钟，推荐）

1. 打开 https://app.netlify.com/drop
2. 把 `E:\Triumph\praxis-5001\` 文件夹**直接拖进网页**（或选文件夹）
3. 部署完给你一个 `https://xxxxx.netlify.app` 链接——就是公开 URL
4. 之后每次改文件重新拖一次即可（或连 GitHub 自动部署）

## 方案 B：Cloudflare Pages（免费 + 国内访问相对稳）

1. 注册 https://dash.cloudflare.com
2. Workers & Pages → Create → Pages → Upload assets
3. 上传整个文件夹，拿 `https://xxxxx.pages.dev` 链接

## 方案 C：GitHub Pages

1. GitHub 建仓库，上传两个 HTML
2. Settings → Pages → 选 main 分支 → 拿 `https://<user>.github.io/<repo>/`
3. 注意：GitHub Pages 慢一些，但长期免费

## 部署前 checklist

- [ ] 浏览器本地打开两个文件，点 CTA 确认互链正常（file:// 下相对路径也能跳）
- [ ] 手机宽度看一眼（响应式）
- [ ] 把问卷建到 Google Forms（链接替换进 `Reddit引流帖.md`）
- [ ] 部署后测试：从部署的 URL 打开 → 点 "Run the free diagnostic" → 完整跑一遍

## 常见坑

- **Netlify 拖文件夹**：整个 praxis-5001 文件夹一起拖，别只拖单个 HTML（两个文件要在一起）
- **中文文件名**：HTML 文件名最好改成英文（如 `landing.html`、`diagnostic.html`）再部署，避免 URL 编码问题——改文件名后记得同步 `Landing Page.html` 里的跳转目标（`window.location.href` 值）
- **file:// 直接双击**：两个文件在同一个文件夹时相对路径跳转正常；部署后同理
