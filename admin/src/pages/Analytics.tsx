import Placeholder from '../components/Placeholder';

export default function Analytics() {
  return (
    <Placeholder
      title="行为分析"
      desc="漏斗 / 留存 / 正确率趋势（P3 接入 Analytics Engine + D1 rollup）"
    />
  );
}
