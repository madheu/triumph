# Triumph · 项目状态快照（2026-08-16 22:10）

> 给明天/新会话的交接：当前进度、下一步、关键路径。

## 已完成（全部验证过）

| 资产 | 路径 | 状态 |
|---|---|---|
| Landing Page v2（莫兰迪风，CTA 互链） | `triumph\index.html`（开发源） | ✅ |
| 诊断 Demo（随机抽题 12/次 + 报告 + 付费墙，对=绿/错=红） | `triumph\diagnostic.html` | ✅ |
| 真实题库 86 题（四批全过验证、审计 0 问题） | `assets\questions-full.json` + diagnostic.html 内嵌 DM_BANK | ✅ |
| 官方蓝图 / PDF / 提取文本 | `assets\official-blueprint-5001.json`、`E:\hermes-workspace\praxis_5001\` | ✅ |
| **部署目录 site/（50px 留白 + SEO 三件套，与 triumph/ 逐字节一致已验证）** | `site/`（index/diagnostic/robots/sitemap） | ✅ **待部署** |
| 发帖素材 | `Reddit引流帖.md`（长版）、`Facebook-Quora发帖文案.md`（短版：FB 3 变体 + Quora 3 回答 + 证据卡模板） | ✅ 待填问卷链接 |
| 问卷内容 | `验证问卷.md`（Q1–Q9，含判定口径） | ✅ 待建 Google Forms |
| **付费意愿判定脚本** | `analyze-survey.js`（CSV → 严格/宽松口径 + ✅/❌，已用模拟数据验证） | ✅ |
| 部署脚本 | `deploy.ps1`（wrangler 直传 triumph-6eq，不依赖 git） | ✅ PARSE OK |
| 同步脚本 | `sync-site.ps1`（triumph/ → site/，非 git 仓库自动跳过 commit） | ✅ 已实测 |
| 出题 skill（已装 Hermes） | `C:\Users\abc27\.hermes\skills\praxis-question-writer\` | ✅ |

## 本轮修复（2026-08-16，文件整理后的路径修复）

- **开发文件位置变更**：`Landing Page.html` / `Diagnostic Demo.html` 已整理进 **`triumph\` 子目录**（index.html / diagnostic.html），快照此前未跟上
- **sync-site.ps1 修复**：源路径指向不存在的根目录文件 → 改为 `triumph\`；git 检测改 try/catch（非 git 仓库跳过 commit）；**补 UTF-8 BOM**（PS 5.1 无 BOM 会把中文按 ANSI 解码导致语法错误，实测踩坑）
- **build-demo-bank.js 修复**：demoPath 改为 `triumph\diagnostic.html`（构建后需 sync-site.ps1 + deploy.ps1）
- **README-deploy.md 重写**：以 wrangler 直传为主流程，GitHub 自动部署降为备选
- **验证结论**：`triumph/` 与 `site/` 两文件 hash 完全一致 → site/ 是最新 86 题版，**可直接部署，无旧题库风险**

## 关键勘误（已修正）

**官方科目代码：5002 = Reading and Language Arts（80 题/90min）、5003 = Mathematics（50 题/65min）**。文案/代码均已按此写。

## 待办（按顺序，前三步都在用户侧）

1. **[用户] 跑 `.\deploy.ps1`** → 预期 "Uploaded 4 files" → 确认 https://triumph-6eq.pages.dev 上线
2. **[用户] 手机验证**：曲面屏 50px 留白不贴边；首页标题/description 正常；诊断页抽 12 题
3. **[用户] 建 Google Forms**（内容复制 `验证问卷.md`）→ 把链接填进 `Reddit引流帖.md` 和 `Facebook-Quora发帖文案.md` 的 `[问卷链接]`
4. **[用户] 发帖**：FB 帖子 A（首选）→ Quora 回答 1、2 → Reddit 长帖；美东工作日 7–10pm；24h 内回评论
5. **[用户+agent] 判定**：Google Forms 导出 CSV → `node analyze-survey.js survey.csv`（判定口径已内置：愿付 $15/mo ≥10 人 且 ≥30%）
6. **[后置·MVP 验证后再谈] 国内题库知识点参考**：5003 数学/5005 科学可用国内教材清单（普适知识），5002/5004 不可用；仅作"知识点覆盖清单"参考，出题仍走 Hermes + skill 原创

## 决策要点（发帖后）

- 付费假设：$15/mo（用户定）；通过率预测是核心差异化
- 版权红线：全原创，真题/回忆题禁用（skill 已内置）
- 验证判定：付费意愿 ≥30% 且 ≥10 人 → 进入正式开发；判定脚本 `analyze-survey.js` 已就绪

## 发帖渠道现状

- ~~Reddit 主板块~~ → 板块级封禁，放弃；候选：Facebook 备考群组 / Quora / 大学教育学院 B2B
- FB/Quora 文案已备好（`Facebook-Quora发帖文案.md`）
- **AtoZ / Practice Test Geeks 论坛有 Praxis 5001 专区（真实 URL 已记入文案文件），看帖免费**；发帖是否收费待用户实测——若免费则是最对口渠道，优先级高于 FB 群组
- 竞品价格锚点：240 Tutoring ~$39/mo、Study.com ~$60/mo → $15/mo 有价格优势

## ⚠️ 编码铁律（踩过坑）

- 所有 `.ps1` 文件必须是 **UTF-8 带 BOM**（Windows PowerShell 5.1 无 BOM 会把中文按 ANSI 解码 → 语法崩溃）
- agent 的 write/edit 工具写出的 ps1 无 BOM → 写完必须用 PowerShell 补 BOM（`[System.IO.File]::WriteAllText($p, $c, [System.Text.UTF8Encoding]::new($true))`）
