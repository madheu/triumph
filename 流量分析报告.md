# Triumph 流量分析报告（Cloudflare GraphQL）

> 数据源：Cloudflare Analytics API（GraphQL），token 只读权限
> 账号：ea1585383ab36bf04cbe995b49285ffc
> 站点：triumph-6eq.pages.dev
> 分析时间：2026-08-16

## 1. 按天请求量

| 日期 | 请求数 |
|---|---|
| 08-11 | 1 |
| 08-12 | 2 |
| 08-13 | 24 |
| 08-14 | 24 |
| 08-15 | 402 |
| 08-16 | 413 |

部署集中在 8/15，流量从 8/15 起放量。两天合计 ~815 请求（你看到的"700 多"吻合，请求数略高于访问数）。

## 2. 按国家（8/13 以来，count 口径）

- **US 969**（主市场 ✓）
- CH 296 / DE 267 / NL 210 / FR 156 / BG 82 / GB 60 / IE 44 / FI 41 / LU 36 / CA 34 / PL 32 / IM 31 / BE 30 / UA 28 / EE 8 / RO 8 / RU 8 / SE 8 / AT 6 / AU 5 / T1 3 / JP 2 / HR 2 / KR 1

⚠️ 瑞士/德国/荷兰/法国/保加利亚等欧洲大流量 = **数据中心/机器人特征明显**（真实美国教师用户不会成批来自欧洲机房）。

## 3. 按页面路径（8/13 以来）

**真实页面**：
| 路径 | 请求数 | 解读 |
|---|---|---|
| `/`（首页） | 665 | 主入口 |
| `/diagnostic` + `/diagnostic.html` | ~119 | 诊断页（**真实用户核心行为**） |
| `/favicon.ico` | 181 | 浏览器自动请求（真实访问信号） |

**机器人/扫描流量**（非真实用户）：
| 路径 | 请求数 | 解读 |
|---|---|---|
| `/wordpress/wp-includes/wlwmanifest.xml`、`//wp-includes` 等 | ~30 | WordPress 漏洞扫描器 |
| `/app-ads.txt` | 21 | 广告配置探测 |
| `/static/style/protect/index.js`、`/bot-connect.js`、`/js/antibot-client.js` | ~50 | 反爬/验证脚本探测 |
| `/.env`、`/app/.env`、`/backend/.env`、`/.env.swp` | ~8 | 环境变量文件扫描 |
| `/server-status`、`/trace.axd`、`/___proxy_subdomain_cpanel` | ~15 | 服务器漏洞探测 |
| `/aa-also-nonexistent-7183.php`、`/zz-nonexistent-test-8492.html` | ~6 | 存在性探测 |

## 4. 结论

1. **真实用户流量存在**：首页 ~665 请求 + 诊断页 ~119 请求 + favicon 181。诊断页按"每人 2-3 请求"估算 ≈ **40-60 个真实访问者**打开了诊断。
2. **机器人大流量**：欧洲数据中心 IP + 大量漏洞探测路径——「700 多访问」中**有相当比例是机器人和扫描器**，Cloudflare 的请求计数把它们全算进去了。
3. **转化漏斗雏形**：首页 665 → 诊断 119（~18% 点击进诊断）——这个转化率对 MVP 验证其实不错。
4. **GA 的必要性坐实**：只有 GA 能过滤机器人、给出真实用户数/行为/来源渠道。Cloudflare 请求数统计会把扫描器算成"访问"。

## 5. 建议

- **立即接入 GA**（等用户 Measurement ID）→ 拿到真实用户数、来源渠道、行为漏斗
- **（可选）开 Cloudflare Bot Fight Mode**（安全 → Bots 设置，免费）→ 自动拦截扫描器，请求数恢复干净
- 静态站被扫描无实际风险（无后端可攻击），可放心

## 6. 数据来源说明

- GraphQL 端点：https://api.cloudflare.com/client/v4/graphql
- 数据集：httpRequests1dGroups（按天）/ httpRequestsAdaptiveGroups（维度聚合）
- 注意：adaptive 数据集的 `sum.requests` 字段在本 token 权限下不可用，改用 `count` 计数（count ≈ 请求数，量级一致）
