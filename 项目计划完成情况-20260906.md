# Learndiag · 计划完成情况检查（2026-09-06 20:30）

> 检查方式：读 HANDOFF.md（8/28 交接）、待办标注.md、8/30–9/6 记忆，并**逐项 grep 线上代码 / 查 KV·D1 / 查 git** 实证。
> 凡标「实证」的都是本轮亲自查过的，不是文档转述。

---

## 一、已完成（实证）

| 项 | 证据 |
|---|---|
| 品牌与域名迁移（learndiag.com，旧域 301） | 线上生效 |
| 真实账户系统（KV `users:*` + JWT，不再是 mock） | KV 4 个用户，PBKDF2 + HS256 |
| Creem 支付接入（测试模式） | HANDOFF §5 |
| Resend 邮件域名验证 | HANDOFF §3 |
| 22 篇 SEO 文章 + 各自 og:image | `site/images/*.png` 22 张 |
| GSC 验证 | DNS 记录 + 老户 9/2 能读到曝光数据 |
| **404 深链根因定位与修复** | 8/31 查明 URL 命名规律（5002 裸名 / 5003-5005 带科目词），已固化进 MEMORY.md |
| **帖文分析器**（7 模块 + 州三档护栏 + 日配额 + 7 天查重） | `Triumph SEO/帖文分析器.html`，12 帖型冒烟通过 |
| dev.to 发帖 | 8/30 已发 |
| Agent discovery + Content Signals | isitagentready 两项 pass |
| **站点状态速查脚本**（自动续 token） | `status.js`，9/6 新建 |
| **git 进度快照已提交** | 9/6 提交 `8622f02`，10 个文件改动入库 |

---

## 二、未完成（实证，按紧急度）

| 级别 | 项 | 实证结果 |
|---|---|---|
| 🔴 | **州及格线数据表 `state_cut_scores`** | 全库 grep `cut_score`/`cutScore` **零命中** |
| 🔴 | **州选择器** | grep `state-select`/`userState` **零命中** |
| 🔴 | **诊断页及格线仍硬编码** | `site/diagnostic.html` 显示 "a typical line sits near **160**"，无用户所在州概念 |
| 🔴 | **分享卡片 canvas 晒图** | 无 `toDataURL`、无 `document.fonts.ready` → PRD 核心（生成可晒图）**未实现** |
| 🟠 | **ShareScore 文案与 PRD 决策冲突** | 现线上晒 `I just scored {分数} (est.) … {pass}% pass probability`，正是 PRD §F5 判定"会被拆穿"的 mock 线性模型输出（每科仅 3 题，只能取 140/153/167/180） |
| 🟠 | **og-image 仍是旧品牌** | `site/og-image.png` 仍为 Triumph + trytriumph.de5.net |
| 🟠 | **Pinterest：claim 域名 + 首批 pin** | 未做；流量数据无任何 P 站来源迹象 |
| 🟠 | **Q1 决策未答**：10-01 后分享卡片是否永久免费 | 8/30 挂起至今 |
| ⚪ | git 历史残留损坏 | 提交成功但 `repack` 报 `Could not read 58b316e`，暂不影响使用 |

---

## 三、需老户确认（我在代码里查不到）

- GSC：sitemap 是否已提交？resources 页是否「请求编入索引」重抓？
- Bing Webmaster 是否提交 sitemap？
- 注册验证码邮件是否实测收到（含垃圾箱）？
- **Show HN**（排 9/1 美东上午）、**Indie Hackers**（排 9/2）、**MCP 目录**（8/31–9/3）——发了吗？
- 8/31–9/6 社区分发日历实际执行了几条？
- FB 评论对外可见性验证（8/30 建议的 3 分钟检查）做了吗？
- 熟练度引擎是否已接真实做题数据？
- Google OAuth 外部（非 owner）账号能否登录？

---

## 四、判断与建议

**1. 最紧的是分享卡片的 9/12 期限——只剩 6 天，两个硬前置一行没写。**
PRD 排期 5.25 人日，其中州线表 0.5d + 州选择器 1d + 结果页接州线 0.5d = 2d 是前置。
**建议砍州线**：PRD 的核心决策本来就是「主打实测正确率 + weakest gate，弱化模型分数」，州线是加分项不是必需项。砍掉后 canvas 渲染 1.5d + 分享面板 1d + 埋点 0.5d ≈ 3d，9/12 可保，且能接住 10-01 前的免费期——那才是病毒循环唯一干净的数据窗口。

**2. 现线上 ShareScore 是个敞口风险，比"没做"更值得处理。**
它现在对外晒的是 mock 模型分数。教师社群对分数极敏感，被拆穿一次信任就没了。要么改文案为「正确率 + weakest gate」（PRD 原决策），要么先下掉。这个改动本身只要半小时。

**3. Pinterest 停在起跑线。**
claim 域名 + 首批 pin 都没动。它 2–4 周才见效——今天不动，10 月照样零。这是目前唯一能带来**增量新入口**的动作（FB 群触达倍率天花板 0.4x，SEO 新域名 2–6 周）。

**4. 数据面：仍处零真实用户。**
9/3 首次出现 20 题答题行为（疑自测），注册仍 4 个测试账号，零转化。所有 Phase 1 验证问题因无样本而答不了。

---

## 五、建议的下一步顺序

1. **决定分享卡片范围**（砍州线 / 推迟 deadline）——今天定，否则 9/12 自动作废
2. **改或下掉 ShareScore 文案**——半小时，消掉对外风险
3. **Pinterest claim 域名**——老户 5 分钟，我配合部署验证文件
4. 发首批 12 张 pin（启动包已备好含 UTM 的落地链接）
5. og-image 换 Learndiag 品牌（待老户点头）
