# Triumph · 诊断模型设计（从 mock 到真实算法）

> 目的：定义真实诊断算法的输入、计算、输出契约，明天真题库到位后替换 Demo 里的
> `DM_estimateScore` / `DM_passProbability` / 薄弱点归因 三个 mock，直接组装成 MVP 核心。
> 状态：设计稿（基于行为地图 + 官方考试结构，未实现）

## 1. 输入契约（来自答题记录）

```ts
interface AnswerRecord {
  questionId: string;
  subtest: '5002' | '5003' | '5004' | '5005';
  category: string;        // 知识点类别，对齐题库模板的 categories
  difficulty: 'easy' | 'medium' | 'hard';
  type: 'concept' | 'procedure' | 'pedagogy';
  correct: boolean;
}
```

## 2. 估计 scaled score（每科 + 总分）

**现实**：真实 Praxis 用 IRT（项目反应理论）评分，分数 100–200。
**我们**：不做 IRT，用"蓝图加权答对率 → 分数映射"的合理近似（原型阶段够用，标注为估计）。

```
每科估计分 subtestScore = 100 + rawRate * 100 × blueprintWeight 修正
  rawRate = 该科答对题数 / 该科作答数
  蓝图权重：按 Study Companion 各类别占比加权（模板 json 的 categories 已对齐）

总分 = 四科平均（5001 无总分，四科独立；总分仅作展示，不作通过判定）
```

**校准锚点**（防拍脑袋）：
- 各州及格线常见区间 150–165（调研确认），Demo 默认演示线 160
- 首考通过率 <50%（NCTQ）→ 分数分布基准应让"平均用力复习者"落在 155–165 之间，才能解释一半人挂科

## 3. 通过概率（每科 + 总分展示）

```
passProbability(score, stateLine) = 1 / (1 + exp(-k × (score - stateLine)))
  k = 斜率（原型取 0.06，校准用历史通过率）
  stateLine = 用户所在州及格线（未选州时用 160 默认）
```

**校准**：用真实数据回头调 k——若某科首考通过率 <50% 而模型给出 >70% 概率，说明斜率/锚点偏乐观。

## 4. 薄弱点归因（核心差异化）

```
1. 每科正确率 → 最低一科 = 最弱门（weakest gate）→ 计划 60% 时间
2. 最弱科内：按 category 正确率排序 → 最弱 2–3 个知识点（供计划/记忆卡定向）
3. 题型诊断：pedagogy 类答对率 vs concept/procedure → 提示"教学法题型薄弱"
4. 边缘判定：任一科 150 < score < stateLine+3 → 标记"临界危险"（复考高风险人群）
```

## 5. 计划生成（规则模板，接行为地图结论）

```
- 每天 1–2 小时 × 每周 5–6 天（调研中位）
- 60% 时间 → weakest gate 的最弱知识点
- 每周 1 次分科限时模拟（调研：模拟考是"过了的人"最高频共同点）
- 考前三节点干预：考前 2–4 周（确定性）→ 模考低分 48h（差距可视化）→ 挂科 72h（重考计划）
```

## 6. 替换点（Demo 内）

| mock 函数 | 位置 | 替换为 |
|---|---|---|
| `DM_estimateScore(subtestAccs)` | Diagnostic Demo.html | 蓝图加权估计（本节 §2） |
| `DM_passProbability(avgScore)` | 同上 | logistic 模型（§3），接州及格线 |
| 最弱科计算（`weakestIdx`） | 同上 | §4 归因 + 知识点级 + 题型级 |
| 周计划（写死的 plan-item） | 同上 | §5 规则引擎生成（按 weakest gate 知识点） |

## 7. 数据依赖（明天题库到位后一起接）

- 题库 `questions.json`（模板已建）→ 提供 category/difficulty/type 字段
- 州及格线表（用户可手填或后续爬各州官网）
- 校准用的历史通过率（NCTQ/机构公开数据，调研已有，后续可选接入）

## 8. 诚实标注

原型阶段所有分数/概率都标"estimate（估计）"，不做"成绩声明"——既是合规（ETS 无关联），也是诚信（避免用户误当官方成绩）。
