# Triumph 流量分析报告（2026-08-20）

> 数据源：Cloudflare GraphQL Analytics（zone: trytriumph.de5.net）+ 历史记录
> 生成时间：2026-08-20
> ⚠️ 数据范围说明：`trytriumph.de5.net` 域名**今天（08-20）才绑定激活**，本报告以该 zone 当日数据 + 此前 pages.dev 时期的已知记录拼接。

---

## 1. 总览

| 指标 | 数值（08-20 当日，de5.net） |
|---|---|
| 总请求 | 249 |
| 页面浏览 | 148 |
| 唯一访客 | 124 |
| 带宽 | 2.0 MB |
| 缓存命中 | 1（新域名缓存未预热） |

> 对比：08-16 报告显示 pages.dev 时期日均 ~400 请求、~700 访问。**今天 de5.net 的 249 请求主要来自：上线活动（部署/测试/你自己）+ 机器扫描**，真实用户占比需结合路径分析判断。

---

## 2. 按国家（08-20）

| 国家 | 请求 | 解读 |
|---|---|---|
| 🇺🇸 US | 186 | 目标市场（教师候选人）✅ |
| 🇨🇳 CN | 143 | 开发者/测试流量（你 + 部署测试） |
| 🇳🇱 NL | 16 | 数据中心特征（机器/扫描） |
| 🇨🇦 CA | 12 | 真实或扫描混杂 |
| 🇩🇪 DE | 6 | 数据中心特征 |
| 其他（BE/SE/FI/SG/TW/BR 等各 1-3） | ~20 | 分散小流量，多为探测 |

**判断**：US 为主 ✅ 符合目标；CN 143 主要是你自己在测试；NL/DE 等欧洲流量是**数据中心/爬虫特征**（真实美国教师不会从荷兰机房来）。

---

## 3. 按路径（08-20）

### 真实页面
| 路径 | 请求 | 解读 |
|---|---|---|
| `/`（首页） | 192 | 绝对主入口 |
| `/resources` | 7 | 资源页 |
| `/praxis-5001-study-guide` | 4 | 总指南 |
| `/diagnostic` | 3 | 诊断（真实用户核心行为） |
| `/praxis-5001-vs-8000-series` | 2+2 | 新文章（SEO 价值） |
| `/praxis-5001-four-gate-strategy` | 2 | 策略文 |
| `/praxis-5001-vs-7001` | 2 | 对比文 |
| `/praxis-5001-retake-guide` | 2 | 重考指南 |
| `/dashboard` | 2 | 面板 |
| `/practice` | 1 | 练习 |
| `/api/register` | 1 | **注册测试**（说明账户系统有人用了） |
| `/api/me` | 1 | API 探测 |

### 机器/扫描流量
| 路径 | 请求 | 解读 |
|---|---|---|
| `/cdn-cgi/challenge-platform/*` | ~40 | Cloudflare 反爬 JS 挑战（正常机制） |
| `/.well-known/acme-challenge/*` | ~30 | **SSL 证书签发验证**（今天域名激活的正常行为） |
| `/.well-known/openid-configuration` / `api-catalog` / `mcp` | ~12 | OIDC/API 扫描 |
| `/lrjwutex` / `/sojlgeza` | ~9 | **随机路径爆破**（恶意扫描） |
| `/robots.txt` | 16 | 爬虫信号（搜索引擎） |
| `/sitemap.xml` | 6 | 爬虫信号 |
| `/favicon.ico` | 24 | 浏览器自动请求（真实访问信号） |

**判断**：真实用户路径（首页→resources→诊断/文章）清晰可见；扫描流量约占总请求 30%，属正常水平（Cloudflare 已拦截）。

---

## 4. 状态码分布

（08-20 未单独拉取，预计：2xx 占多数 + 404 扫描 + 403 拦截）

---

## 5. 与 08-16 报告对比（pages.dev 时期）

| 维度 | 08-11~08-16 | 08-20（de5.net） |
|---|---|---|
| 日请求峰值 | 413 | 249 |
| 主流量国 | US 969 | US 186 + CN 143 |
| 首页请求 | 665 | 192 |
| 诊断页 | ~119 | 3 |
| 机器流量 | WP 扫描 ~30 | 随机路径爆破 ~9 |

**趋势**：pages.dev 时期日均 ~400 请求大部分是**机器流量**（此前报告已判定欧洲机房流量异常）；今天 de5.net 的真实用户路径占比更高，质量更好。

---

## 6. 结论与建议

### 流量质量判断
- ✅ **目标市场正确**：US 为主
- ✅ **真实用户行为存在**：首页→诊断/资源路径清晰
- ⚠️ **总量很小**：124 唯一访客含你自己和测试流量，**真实独立用户可能 < 30**
- ⚠️ **SEO 尚未起量**：文章页单篇个位数访问，Search Console 收录还在路上

### 建议动作
1. **等 48-72 小时再看**：域名刚激活 + GSC 刚提交，SEO 流量需要时间
2. **监控注册转化**：今天已有 `/api/register` 触发（账户系统生效），关注验证码完成率
3. **清理扫描噪音**：Cloudflare 已自动拦截，无需操作；后续报告可加 `clientRequestPath NOT LIKE '%.well-known%'` 过滤
4. **GA4 作为交叉验证**：GA 数据（真实用户数）应比 CF 请求数低很多，两者对比能更准确判断真实流量

### 数据缺口（如需完整历史）
- **pages.dev 8/15-8/19 的流量**需要带 Pages 读权限的 token（当前 token 只有 zone 权限）
- GA4 报告需要 GA API 凭据

---

## 附：数据拉取脚本
`fetch-traffic.js` — 用 Cloudflare GraphQL API 拉取，支持按天/国家/路径/状态码查询。
用法：`node fetch-traffic.js <CF_TOKEN> <天数>`
