# Triumph 后台管理系统 & 收银台 · 总体规划 v1.0

> 状态：规划稿（待评审）
> 范围：后台管理（内容/账号/退款/行为分析）+ Creem 收银台
> 约束：单人运营 · 全 Cloudflare 生态 · 分阶段交付 · 验证期 < 1000 用户

---

## 0. 一句话方案

> **在现有 Pages 项目里长出一个模块化 Worker 后端 + D1 关系库，后台用 React SPA 构建到 `/admin/`，收银台直接挂 Creem 托管 Checkout——不引入第二套运行时，不引入第二朵云。**

---

## 1. 现状盘点（2026-08 核实过的事实）

| 项 | 现状 | 来源 |
|---|---|---|
| 前端 | 纯静态 HTML/vanilla JS，diagnostic 页内嵌 React(CDN)；页面含 practice/srs/dashboard/resources/login | `site/` |
| 后端 | **`site/_worker.js` 单文件 Pages Worker（~69KB）**，路由：register / verify / resend / login / me / logout / state | 已核实 |
| 存储 | 仅 KV（`TRIUMPH_KV`）：`users:{email}`、`state:{id}`、`verify:{email}` | `wrangler.toml` |
| 邮件 | Resend，发件人 `verify@learndiag.com` | 同上 |
| 支付 | 无（paywall 是 demo 占位："Prototype — payment not wired."）；**Creem 注册审核中** | `diagnostic.html` L1155 |
| 题库 | `assets/questions-full.json`（969 题 v3.0）→ 构建成 `site/questions.js` 静态加载 | assets/ |
| 文章 | `site/md/*.md` → 手工转 HTML 的流水线（有 `md-to-article.js`） | site/md/ |
| 账号缺口 | **无密码重置**（forgot/reset 不存在）、无订阅字段、无角色概念 | `_worker.js` 路由核实 |
| 版本控制 | 本地 `.git` 已存在；部署走 `deploy.ps1`（wrangler 直传，非 git 连接） | praxis-5001/ |

> ⚠️ 注意：根目录 README 和部分文档已落后于实际代码（README 还写着"纯前端"），本节以代码核实为准。

---

## 2. 需求清单（已确认）

1. **收银台**：Creem（MoR，审核中）承接 $15/mo 订阅；用户侧托管 Checkout + 客户门户
2. **账号管理**：密码重置（用户自助 + 管理员代办）、订阅状态显示
3. **退款**：工单制——用户提交工单 → 后台批准 → 调 Creem 退款 → 撤销权益 + 邮件通知
4. **内容管理**：题库（969 题 → 持续扩库）、SEO 资源文章、学习计划/知识地图、邮件文案
5. **学习工具可插拔**：未来新增工具不允许硬编码，要有注册机制
6. **用户行为分析**：留存时长、干了什么（路径/漏斗）、做题正确率
7. **后台使用者**：只有你一个人，不需要 RBAC，但需要操作审计日志

---

## 3. 总体架构

```
浏览器
 ├─ 用户站 site/*.html ── fetch /api/* ────────────────┐
 └─ 管理后台 site/admin/* (React SPA) ─ fetch /api/admin/* ─┤
                                                        ▼
                                    Cloudflare Pages Worker（模块化 TS 构建）
                                     ├─ src/api/auth.ts        （现有，迁移）
                                     ├─ src/api/state.ts       （现有，迁移）
                                     ├─ src/api/billing.ts     ★ 新：checkout/webhook/portal
                                     ├─ src/api/password.ts    ★ 新：忘记/重置密码
                                     ├─ src/api/admin/*.ts     ★ 新：后台 API（门禁中间件）
                                     ├─ src/api/track.ts       ★ 新：行为埋点接收
                                     └─ src/lib/: kv.ts d1.ts creem.ts resend.ts ae.ts audit.ts
                                                        │
              ┌───────────────┬───────────────┬────────┴────────┐
              ▼               ▼               ▼                 ▼
        Cloudflare KV    Cloudflare D1   Analytics Engine    外部 SaaS
        会话/验证码      ★关系主库        行为事件流          Resend（邮件）
        内容发布快照     用户/订单/订阅   (10万点/天 免费)     Creem（支付/MoR）
                        题库/工单/审计
                        内容登记表/工具注册表
```

**关键决策与理由：**

| 决策 | 选择 | 理由 |
|---|---|---|
| 后台部署位置 | 与主站同一个 Pages 项目，SPA 在 `/admin/` 子路径 | 一个部署单元、零额外成本；模块边界划清后随时可拆出独立项目。配合 Cloudflare Access（免费 ≤50 人）做网络层门禁 |
| `_worker.js` 形态 | 重构为多文件 TS 源码 + esbuild 打包回单文件 | 现在 69KB 单文件已到维护临界点；加计费/后台/埋点必然失控。Pages 高级模式支持目录形式，wrangler 自动打包 |
| 关系存储 | 引入 D1 作为订单/订阅/题库/工单/审计的主库 | KV 无法做查询/过滤/聚合——后台的核心诉求恰恰是"查"；KV 退守它擅长的 TTL 场景（会话、验证码、缓存） |
| 用户表迁移 | Phase 1 双写过渡（KV 为准 + D1 镜像），Phase 2 切 D1 为主 | <1000 用户迁移成本极低，但没必要和收银台挤在一个阶段冒险 |
| 行为事件 | Analytics Engine 存原始事件流 + D1 存做题结果精确汇总 | AE 免费层 10 万点/天对验证期绰绰有余；但 AE 是采样型，正确率这类"要精确数字"的指标落 D1 日汇总表 |

---

## 4. 后台管理技术栈分析（核心章节）

### 4.1 候选方案对比

| 方案 | 优点 | 缺点 | 结论 |
|---|---|---|---|
| **A. React 18 + Vite + TS + Tailwind + shadcn/ui + TanStack** | 生态最大；shadcn 组件源码直接进仓库、完全可控；TanStack Query/Table 把列表-筛选-分页-缓存这些后台刚需标准化；图表 Recharts 成熟 | 样板代码比 Vue 多一点（用 shadcn CLI 大幅缓解） | ✅ **推荐** |
| B. Vue 3 + Element Plus + Pinia + ECharts | 中文文档一流；Element 的表格/表单/弹窗开箱即用，拼后台最快 | 全球生态与 AI 代码生成质量略逊于 React；组件定制深度受限 | 合格备选，若你更熟 Vue 就用它 |
| C. react-admin / refine 这类后台框架 | CRUD 资源化配置，列表页白送 | 自定义分析页、工单流、Creem 操作面板时要"跟框架搏斗"；AI 生成时框架特定 API 幻觉率高 | ❌ 不选：我们的页面一半是定制视图 |
| D. 低代码（Retool/Appsmith） | 最快出活 | Retool 收费且逻辑跑在别人云上；Appsmith 要自己找服务器（违反全 CF 约束）；Creem 退款流、自定义 SQL 看板做不了 | ❌ 违反约束 |
| E. Hono JSX SSR + HTMX（纯 Worker 渲染） | 极简、CF 原生、零前端构建 | 富交互（Markdown 编辑器、图表、行内编辑）体验吃力，迭代慢 | ❌ 后台是重交互场景，不匹配 |
| F. Next.js 全栈后台 | 生态好 | 需要 SSR 运行时/OpenNext 适配，与现有 Pages 直传部署冲突；后台不需要 SEO/SSR，纯属多余抽象 | ❌ 过度设计 |

### 4.2 推荐栈明细（方案 A 展开）

```
前端（后台 SPA）
  React 18 + TypeScript + Vite          构建/语言基座
  Tailwind CSS + shadcn/ui              样式与组件（按钮/表格/对话框/Toast）
  TanStack Query                        服务端状态（请求缓存/失效重取）
  TanStack Table                        用户列表/订单表/题库表的排序筛选分页
  React Hook Form + Zod                 表单 + 校验（Zod schema 前后端共用）
  Recharts                              留存曲线/漏斗/正确率趋势图
  react-router                          后台内路由
  输出：site/admin/（静态资产，随主站一起 wrangler pages deploy）

后端（同一 Worker 内）
  TypeScript + esbuild                  wrangler 打包，源码在 src/
  Hono（可选）                           若嫌手写路由分发麻烦可引入，仅做路由层
  Zod                                   所有 /api 入参校验（与后台前端共用类型）
  Vitest + miniflare                    worker 单测/本地集成测试（test/ 已有雏形）
```

**为什么这套最适合"AI 辅助的单人开发"：**
1. React + shadcn/TanStack 是当前大模型训练语料最充分的组合，AI 生成代码的可用率显著高于小众栈；
2. shadcn 组件是拷贝进仓库的源码而非黑盒依赖，AI 可以直接改；
3. Zod schema 前后端共用一份，改一个字段两端同步，单人维护成本最低；
4. 全部落在 Cloudflare 免费层，边际成本为零。

### 4.3 后台信息架构（一期页面清单）

```
/admin
 ├── 登录页                    （复用主站账号体系 + role=admin 校验）
 ├── 概览 Dashboard            今日 UV/PV、新注册、活跃订阅、MRR、异常告警
 ├── 用户管理
 │    ├── 用户列表             搜索/筛选（邮箱、注册时间、订阅状态、最后活跃）
 │    └── 用户详情             资料 + 订阅时间线 + 做题统计 + 状态读写
 │         ├── [重置密码]       生成一次性链接 → Resend 发信（管理员永不接触哈希）
 │         └── [手动授予/取消 Pro]  客服性操作，写审计日志
 ├── 订单与订阅                订单列表、订阅生命周期、[发起退款] 按钮
 ├── 工单                      工单列表 → 详情对话 → [批准退款] 动作
 ├── 内容管理
 │    ├── 题库                 分类×难度×状态筛选、编辑器、上下架、批量导入(B*.json)
 │    ├── 文章/资源            Markdown 编辑器 + 预览 → 发布（提交 GitHub → Actions 自动部署）
 │    ├── 计划模板/知识地图     结构化 JSON 编辑
 │    ├── 邮件文案             模板列表 + 变量占位符校验 + 测试发送
 │    └── 工具注册表           工具卡片：启用开关、可见人群(free/pro)、排序、配置 JSON
 └── 行为分析
     ├── 漏斗                  访问→诊断完成→解锁点击→注册→支付
     ├── 留存                  按首访周 cohort 的 W1-W4 回访曲线
     └── 学习质量              各知识点正确率趋势、日做题量、SRS 复习热力
```

---

## 5. 收银台设计（Creem）

### 5.1 已核实的 Creem 能力

- **MoR 模式**：税务合规（VAT/GST）、拒付处理由 Creem 承担，我们是"从它那里分账"
- API：`https://api.creem.io/v1`（生产）/ `test-api.creem.io/v1`（沙箱），鉴权 `x-api-key`
- 托管 Checkout 页面 + **客户门户**（用户自助换卡/取消）
- Webhook 全事件推送，签名 = HMAC-SHA256(raw body, secret)，header `creem-signature`
- 事件：`checkout.completed`、`subscription.active/paid/canceled/scheduled_cancel` 等
- 有官方 TypeScript SDK

### 5.2 支付链路

```
[dashboard/practice 页] 升级按钮（登录态）
   → POST /api/billing/checkout        （带 Bearer token）
   ← { url }                           （Worker 创建/复用 Creem customer，返回托管结账页）
   → 跳转 Creem Checkout → 支付成功 → Creem 回跳 success_url

异步（权威通道）：
Creem → POST /api/billing/webhook
   ├─ 验签（HMAC-SHA256，raw body）
   ├─ 幂等去重（webhook_events 表，creem event id 唯一）
   ├─ 落库：orders / subscriptions 表 upsert
   ├─ 权益生效：users.plan='pro'，KV 缓存失效
   └─ track('payment_success') → AE

用户侧门户：POST /api/billing/portal → 返回 Creem 客户门户 URL（换卡/取消/发票）
```

**要点：**
- 权益判定永远以 **D1 subscriptions 表 + webhook** 为准，前端 `/api/me` 只是展示；绝不信客户端回调参数
- 审核期间全程用 **test mode** 开发联调，拿到正式 key 只换 secret 即可上线
- `subscription.canceled/scheduled_cancel` 到期未续费 → 定时任务（Cron Trigger 每小时）扫描过期订阅降级

### 5.3 退款流（工单制，按你确认的流程）

```
用户 contact.html 提交工单（原因/订单号）
   → tickets 表 (status=open, refund_requested=true?)
后台工单详情页：
   [批准退款] → POST /api/admin/refunds
       ├─ 调 Creem 退款（API；若 API 未覆盖该场景则引导到 Creem Dashboard 手动退，记录凭证）
       ├─ refunds 表落库（creem_refund_id, reason, operator）
       ├─ webhook refund.* 回来 → orders 状态翻转 + users.plan 降级 + 会话权限即时回收
       └─ Resend 发退款确认邮件
   [驳回] → 工单回复模板
所有动作写 admin_audit_log。
```

---

## 6. 账号管理设计

### 6.1 补齐密码重置（现状缺失）

```
用户侧自助：
  POST /api/password/forgot {email}     → 无论存在与否统一返回 200（防枚举）
                                          生成 token（随机 32B，KV TTL 30min，绑定 email）
                                          Resend 发重置链接 https://…/reset.html?token=…
  POST /api/password/reset {token,new}  → 校验 token → 更新 bcrypt 哈希 → 吊销该用户全部会话

管理员代办：
  POST /api/admin/users/:id/password-reset-link
  → 生成同样的一次性 set-password 链接直接发给用户邮箱（管理员看不到也不需要看到旧密码）
```

### 6.2 订阅显示

- `users` 增加 `plan / subscription_status / current_period_end / creem_customer_id`（镜像自订阅表）
- `/api/me` 返回上述字段 → `dashboard.html` 显示 Pro 徽章与到期日；过期自动回落 free
- 后台用户详情展示订阅时间线（每次 paid/canceled/refund 事件一条）

### 6.3 角色与门禁（单人极简版）

- `users.role`: `'user' | 'admin'`（你的账号手工置为 admin）
- `/api/admin/*` 中间件：验 Bearer token → 查用户 → `role==='admin'` 否则 404（不暴露端点存在）
- 每个写操作追加 `admin_audit_log(actor, action, target, detail, ts)`
- 二阶段加固：Cloudflare Access 套在 `/admin/*` 路径（Zero Trust 免费），即使 token 泄露还有网络层一道门

---

## 7. 内容管理与「可插拔工具」设计

### 7.1 内容分治（不为统一而统一）

| 内容 | 存储 | 发布管道 | 理由 |
|---|---|---|---|
| 题库 | **D1 `questions` 表** | 直接生效；免费练习仍走构建期生成的静态 `questions.js`（SEO/首屏速度不变），个性化抽题走 API | 后台需要按分类/难度/状态查询筛选，这是 D1 的主场；扩库导入脚本直插 D1 |
| SEO 文章 | **git 里的 md（维持现状）** | 后台富文本/MD 编辑器 → 预览 → 「发布」= GitHub Contents API 提交 → Actions 跑 sync+deploy | 现有 md→html 流水线已被验证，SEO 页保持纯静态最优；git 天然版本化 |
| 学习计划/知识地图 | D1 `content_items`(type=plan/map) | 发布 → 渲染快照推 KV → 前端拉 JSON 快照 | 结构化数据，改动频繁，不适合走 git |
| 邮件文案 | D1 `content_items`(type=email) | 发布即生效（Resend 模板引用） | 同上 |
| 全部内容 | `content_revisions` 表留每次修改快照 | 后台可回滚 | 替代 git history 的安全网 |

### 7.2 可插拔学习工具（回应你的硬编码顾虑）

**原则：站点内置的是「工具容器」，工具本身是自注册的数据 + 模块。**

```ts
// ① 工具契约（types/tool.d.ts）
interface TriumphTool {
  id: string;
  mount(el: HTMLElement, ctx: ToolContext): Promise<void>; // ctx: 用户态/权益/API 封装/i18n
  unmount(): void;
}

// ② 工具注册表 = D1 tools 表（后台可增删改，不发版即生效）
// { slug, name, description, icon, entry:"/tools/fraction-drill/index.js",
//   audience:"free"|"pro", enabled, sort, config_json }

// ③ 前端「工具中心」页 = 渲染注册表卡片，点进动态 import(entry) 挂载
// ④ 新工具上线流程：
//    开发工具模块（独立 bundle，符合契约）
//    → 构建产物放入 site/tools/{slug}/
//    → 后台注册表新增一行（名称/图标/人群/配置随意调）
//    → 开关拨 enabled，前台即刻出现/消失
```

这样"加工具"从"改站代码发版"变成"投递模块 + 后台点一下"，配置类调整（文案、排序、开放人群）完全不用碰代码。

---

## 8. 用户行为分析设计

### 8.1 采集

```
前端 tracker（~2KB js/auth.js 同款封装）
  ├─ 匿名期：localStorage anon_id；登录后事件自动带上 user_id（漏斗才能打通）
  ├─ navigator.sendBeacon('/api/track', {...})   （零阻塞、关页不丢）
  └─ 事件字典（v1 只收这 12 个，宁缺毋滥）：
     page_view / diag_start / diag_complete{score} / unlock_click /
     signup / verify_done / login / checkout_start / task_complete{n,correct} /
     srs_review{grade} / resource_read{slug} / tool_use{slug}

Worker /api/track
  ├─ AE.writeDataPoint(blobs:[type, path, anon_id, user_id, extra], doubles:[value])
  ├─ 做题类事件同时累加 D1 attempts_daily(date,user_id,subtest,category,attempted,correct)
  └─ 服务端事件（login/payment_success）由对应 handler 直接打点，保证可信
```

### 8.2 看板 ↔ 数据源映射

| 看板 | 数据源 | 说明 |
|---|---|---|
| 今日 UV/PV、来源 | AE SQL | 实时 |
| 转化漏斗（访问→诊断完成→解锁点击→注册→支付） | AE SQL（按 anon/user 序列） | 验证期北极星：诊断完成→解锁点击转化率 |
| 留存 cohort（W1-W4 回访） | AE SQL 或 Cron 夜间物化到 D1 | 对应策略层的"周复访率 ≥30%"验证指标 |
| 正确率趋势（分类×日） | D1 attempts_daily | 精确值，不受 AE 采样影响 |
| GA4 | 保留不动 | 继续做营销归因，产品内行为以自有埋点为准 |

---

## 9. D1 核心表草案

```sql
-- 身份与权益
users(id TEXT PK, email TEXT UNIQUE, pw_hash TEXT, role TEXT DEFAULT 'user',
      plan TEXT DEFAULT 'free', subscription_status TEXT, current_period_end INTEGER,
      creem_customer_id TEXT, created_at INTEGER, verified_at INTEGER, kv_legacy INTEGER)
subscriptions(id TEXT PK, user_id TEXT, creem_subscription_id TEXT UNIQUE,
      status TEXT, plan TEXT, current_period_end INTEGER,
      created_at INTEGER, canceled_at INTEGER)
orders(id TEXT PK, user_id TEXT, creem_order_id TEXT, amount_cents INTEGER,
      currency TEXT, type TEXT /*payment|refund*/, status TEXT, raw_json TEXT, created_at INTEGER)
webhook_events(id TEXT PK /*creem event id*/, type TEXT, payload TEXT,
      received_at INTEGER, processed_at INTEGER)

-- 工单与退款
tickets(id TEXT PK, user_id TEXT, subject TEXT, status TEXT DEFAULT 'open',
      refund_requested INTEGER DEFAULT 0, created_at INTEGER, resolved_at INTEGER)
ticket_messages(id TEXT PK, ticket_id TEXT, author TEXT /*user|admin*/,
      body TEXT, created_at INTEGER)
refunds(id TEXT PK, order_id TEXT, creem_refund_id TEXT, reason TEXT,
      status TEXT, operator TEXT, created_at INTEGER)

-- 内容与工具
questions(id TEXT PK, external_id TEXT UNIQUE, subtest TEXT, category TEXT,
      difficulty INTEGER, stem_md TEXT, options_json TEXT, answer_idx INTEGER,
      explanation_md TEXT, status TEXT DEFAULT 'active', source_batch TEXT,
      created_at INTEGER, updated_at INTEGER)
content_items(id TEXT PK, type TEXT /*plan|map|email*/, slug TEXT UNIQUE,
      title TEXT, status TEXT, body_md TEXT, meta_json TEXT, updated_by TEXT, updated_at INTEGER)
content_revisions(id TEXT PK, item_id TEXT, version INTEGER, snapshot_json TEXT,
      edited_by TEXT, created_at INTEGER)
tools(slug TEXT PK, name TEXT, description TEXT, icon TEXT, entry TEXT,
      audience TEXT DEFAULT 'free', enabled INTEGER DEFAULT 0, sort INTEGER,
      config_json TEXT, updated_at INTEGER)

-- 行为与审计
attempts_daily(date TEXT, user_id TEXT, subtest TEXT, category TEXT,
      attempted INTEGER, correct INTEGER, PRIMARY KEY(date,user_id,subtest,category))
admin_audit_log(id TEXT PK, actor TEXT, action TEXT, target_type TEXT,
      target_id TEXT, detail_json TEXT, ts INTEGER)
```

索引按查询模式再补（questions 的 category+difficulty+status 组合索引等）。KV→D1 用户迁移写一次性导出脚本，P1 先双写，P2 切主。

---

## 10. 分阶段交付计划

| 阶段 | 内容 | 出口标准 | 预估 |
|---|---|---|---|
| **P0 地基** | `_worker.js` 模块化重构（TS 源码 + esbuild，行为零变更）；D1 建库建表 + 题库导入脚本；后台 SPA 骨架挂上 `/admin/`（登录门禁 + 空壳布局 + 审计日志表就位） | 主站功能回归通过；`/admin/` 能登进去看到空 Dashboard | 2-4 天 |
| **P1 钱路子** | Creem test mode 全链路（checkout/webhook/portal）；订阅表 + `/api/me` 权益字段；前台升级按钮真实化；密码找回/重置；后台用户列表+详情+代重置；订单页 | 沙箱完成"注册→诊断→升级→取消"全流程；重置密码邮件可收到 | 1 周 |
| **P2 内容管理** | 题库后台 CRUD+筛选+批量导入；文章 MD 编辑器（GitHub 发布管道）；计划/文案 CRUD；**工具注册表 + 工具中心动态加载**（拿现有 practice/srs 改造为首批两个"插件"示范） | 不发版新增/下架一个工具；不发版改一篇文章并发布 | 1-2 周 |
| **P3 数据闭环** | 前端埋点 SDK + 12 事件；AE 看板三张；D1 正确率汇总看板；工单系统 + 退款流打通 | 后台能看到真实漏斗和 cohort；一笔沙箱退款走完工单→退款→降权→邮件全链路 | 1-2 周 |

每阶段独立可上线、可停在任何地方不欠债。Creem 审核通过前 P1 全程用沙箱，通过后只换密钥。

---

## 11. 安全要点清单

1. Creem webhook：raw body 验签 + event id 幂等表，拒绝重放
2. 密码找回链接：一次性 token、30 分钟 TTL、用后即焚、重置成功吊销全部旧会话
3. 忘记密码接口对存在/不存在邮箱统一响应（防枚举）
4. `/api/admin/*` 404 化处理 + 审计日志全覆盖 + 建议 P2 起 Cloudflare Access 兜底
5. Creem API key / webhook secret / Resend key 全部走 Workers Secrets，绝不进前端包
6. 权益以后端 D1 为唯一事实源，前端徽章只是展示

## 12. 成本估算（验证期）

| 项 | 费用 |
|---|---|
| CF Pages/Workers/KV/D1/AE | **$0**（免费层：D1 5GB、AE 10 万点/天，均远超 <1000 用户所需） |
| Resend | $0（免费 100 封/天，验证码+通知够用） |
| Creem | 按 MoR 抽成（审核通过后确认费率，通常 ~5%+$0.x/笔，含税务合规服务） |
| **合计固定成本** | **≈ $0/月**（用户量起来后再评估 Workers Paid $5/mo） |

## 13. 已拍板的决策（2026-08 评审通过）

1. **GitHub 远程仓库已有** ✅ → 文章发布管道定为：后台编辑器 → GitHub Contents API 提交 → Actions 自动部署（P2 落地）。
2. **免费边界（重要变更，覆盖策略层旧稿）**：**诊断、Dashboard、练习、每日任务、知识地图全部免费**；当前没有付费内容，**未来更新的内容与新学习工具大部分为会员制**。
   - 架构含义：权益闸门的主开关就是 **工具注册表 `tools.audience` 字段**（free/pro）+ 未来内容的 `content_items.meta_json.audience`，收银台卖的是"未来 Pro 工具的解锁权"；
   - ⚠️ 遗留待定：**P1 上线时收银台的售卖标的**（建议：早鸟 Pro——一次付费锁定未来全部 Pro 工具 + 创始人徽章），需在 P1 动工前定文案与定价（沿用 $15/mo 还是买断制另议）；
   - 漏斗埋点相应调整：`unlock_click` 事件改为 `pro_teaser_click`（预告位点击），支付转化看板等首个付费标的上架后再启用。
3. **题库分治同意** ✅：免费刷题继续走静态 `questions.js`（快、SEO），个性化抽题走 API。
4. **后台入口 = 主域 `/admin/` 路径** ✅：同源零 CORS 配置面、比子域名少一条 DNS 暴露面；Cloudflare Access 可按路径套用，安全性与子域名方案等同或更优。
