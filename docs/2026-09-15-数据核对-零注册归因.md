# 数据核对：GA4 授权状态与「零注册」归因

> 2026-09-15 · 纯只读诊断，未修改任何生产数据
> 数据源：GA4 property `550043894` · D1 `product_events` · KV `users:*`

## 结论摘要

1. **GA4 授权正常**，服务账号有效，property 可读可编辑。
2. **「没有新注册用户」是真的**，不是统计故障 —— 三个独立数据源结论一致。
3. **注册功能本身健康**，不是坏了；断点在漏斗第二级：**89% 的访客只看一眼首页就走**。
4. 顺带发现 **GA4 三个数据流重复计数**，`newUsers` 指标虚高 2.5 倍，不可用。

---

## 一、授权状态

| 项 | 值 |
|---|---|
| 服务账号 | `learndiag-analynze@learndiag-web.iam.gserviceaccount.com` |
| 凭据文件 | `E:\Triumph\secrets\ga-sa.json` |
| Property | `properties/550043894`（displayName = triumph，`canEdit` = true） |
| 属性时区 | **America/Los_Angeles**（看日期要换算，差 15 小时） |
| 属性创建 | 2026-08-16 |
| 网络 | googleapis 需走代理 `127.0.0.1:7890` |

> 附带修复：Cloudflare wrangler token 已过期（09-14 14:21 UTC），已刷新至 09-15 15:54 UTC。
> 这是两套独立凭据，CF token 过期不影响 GA4。

## 二、零注册 —— 三方独立印证

| 数据源 | 最近一次注册 | 空窗 |
|---|---|---|
| GA4 `sign_up` 事件 | 09-06（美西）= UTC 09-07 | 之后 9 天 0 |
| D1 `signup_success`（服务端埋点） | **全历史 0 条** | 该埋点疑似从未生效 |
| KV `users:` 记录（**权威**） | 09-08 23:31 UTC | **7 天 0** |

**全站累计注册账号 = 6 个**（KV 权威；D1 `users` 表只有 3 行，镜像漏了 3 个）：

| 注册时间 (UTC) | plan | verified | 备注 |
|---|---|---|---|
| 08-20 18:09 | pro | True | `admin_granted`（自有号） |
| 08-21 17:39 | — | True | |
| 08-23 15:00 | pro | True | `sub=active` —— **唯一真实付费订阅** |
| 09-05 01:20 | — | **False** | 未完成验证 |
| 09-07 05:16 | — | True | 域名像 SEO 工具站 |
| 09-08 23:31 | pro | True | `admin_granted`（自有号） |

### 注册功能健康（不是坏了）

```
POST /api/register      → 400 invalid_email   ✓ 校验层工作
POST /api/login         → 401 invalid_credentials
POST /api/magic/request → 400 invalid_email
```

> ⚠️ 路由名是 `/api/register`、`/api/login`、`/api/magic/*`；
> **只有 Google OAuth 走 `/api/auth/google`**。用 `/api/auth/register` 探测会得到 404，误判成「接口没了」。

## 三、真正的问题：漏斗第二级就断了

**09-10 → 09-15（6 天）**

| 步骤 | 人数 | 占新访客 |
|---|---|---|
| 新访客 | 137 | 100% |
| 看过题库/测试页 | 15 | 10.9% |
| 开始答题 | 5 | 3.6% |
| 完成诊断 | 4 | 2.9% |
| **注册** | **0** | **0%** |

服务端侧更直白（D1，全期）：

- 有服务端事件的 visitor 只有 **65 个**（GA4 同期报 722 用户）
- 答过 ≥1 题：**21 人**；答过 ≥10 题：**11 人**
- 持续使用的只有 **2 个**（64 题 / 48 题，大概率含自测）

**注册为 0 是第二级断掉的结果，不是原因。**

## 四、数据质量债（新发现）

| # | 问题 | 证据 | 影响 |
|---|---|---|---|
| 1 | **三个数据流重复计数** | 三流 newUsers 相加 709+600+531 = **1840 = 总 newUsers**；sessions 相加 2265 vs 实际 951 | `newUsers` 虚高 2.5 倍，不可用 |
| 2 | 线上只装 `G-MSR1Q1G7W9` | 源码 98 处，48/50 页；另两个 ID 只在 docs | 三流需归档 |
| 3 | host 污染 | `127.0.0.1`(5 会话)、`www.producthunt.com`(1)、已删域名 `praxis5001.com`(43 会话) | 读数据要过滤 |
| 4 | 自动化流量混入 D1 | 09-13 04:27–04:58 八个 visitor 数分钟内密集出现，全 `direct` + 8006 页 | 那 8 条 `signup_prompt_view` 是 QA 流量，「弹窗 8 次 0 转化」是**假信号** |
| 5 | `signup_prompt_view` 仅服务端有 | GA4 侧无此事件 | 两端埋点不对称 |
| 6 | `signup_success` 从未进 D1 | 全历史 0 条 | 缺一条独立校验注册的通路 |

## 五、渠道实况（近 7 日）

| 来源 | 会话 | 占比 |
|---|---|---|
| 直接访问 | 170 | 56% |
| Facebook 系 | 82 | 27% |
| 未归类 | 28 | 9% |
| **Google 自然搜索** | **11** | **4%** |
| X / Twitter | 8 | 3% |
| AI 助手（ChatGPT/Copilot） | 5 | 2% |

- 自然搜索几乎没起量（8-28 才迁域，正常）
- 170 次「直接访问」里多数很可能是 Facebook 内置浏览器（referrer 丢失记为 direct）
- **当前增长靠社交分发，不靠搜索**

## 六、建议（按优先级）

| 优先 | 事项 |
|---|---|
| **P0** | 查「首页 → 题库」这一步为什么留不住人：题库入口是否可见、是否需要登录才给看、首屏加载是否过慢。**89% 的流失在这里。** |
| P1 | 归档 / 删除另外两个 GA4 数据流，停止重复计数；日后报表统一用 `totalUsers` |
| P1 | 给 D1 加 `signup_success` 服务端埋点，让注册可被独立校验（目前只有 KV 能查） |
| P2 | 修 `users` 表与 KV 的镜像同步（漏 3 个） |
| P2 | GA4 加 `signup_prompt_view`，与服务端对齐 |
| P2 | 过滤 `127.0.0.1` / 预览域 / 自动化访客，避免污染报表 |

---

**复现方式**：见技能 `learndiag-data-crosscheck`（含三套通道的脚本与已知坑）。
