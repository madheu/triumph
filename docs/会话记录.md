# 会话记录

> 每个 agent 会话结束时，在此追加一条，方便换会话/交接时快速了解历史。
> 格式参照 `docs/agent协作工作流.md` 第 6 节。

---

## 2026-08-23 · 文档体系建设 + worker 模块化（P0）

**做了什么**：
1. 编写三份维护文档（本文件所在 `docs/` 目录）：
   - `维护手册.md`（非技术读者版项目说明/部署/排查）
   - `代码审查流程.md`（改动前中后检查 + 回滚 + 审查记录）
   - `agent协作工作流.md`（人/agent 分工 + 会话锁 + 文档同步）
2. P0 地基（worker 模块化）✅ 已完成：
   - 将 `site/_worker.js`（1590 行单文件）模块化拆分到 `worker-src/`（11 个模块：worker/http/bank/crypto/keys/accounts/public-api/content/mcp/openapi/constants）
   - 新增 `build-worker.mjs`（esbuild 打包 → 生成 `site/_worker.js`）
   - `deploy.ps1` 增加构建步骤；`package.json` 增加 `build:worker`、`test:parity` 脚本并修复 `test` 脚本
   - 安装 esbuild（已批准构建脚本）

**验证状态（全部通过）**：
- 基线测试：`npm test` = 51 项通过 / 0 失败 ✅（46 原有 + 5 项新增 admin 门禁测试）
- 导出契约：default + 6 个命名导出（parseAccept/negotiatePageVariant/varyWithAccept/MD_ROUTES/openApiSpec/specToYaml）全部保留 ✅
- 行为对拍：`test/diff-worker.mjs`（新旧 worker 对比，9 组请求）—— 全部相同 ✅（已收进 `npm run test:parity`）

**P0 地基 ✅ 全部完成（含 D1 与后台骨架）**：
- **D1**：`db/schema.sql`（12 张表：users/subscriptions/orders/webhook_events/tickets/ticket_messages/refunds/questions/content_items/content_revisions/tools/attempts_daily/admin_audit_log）+ `tools/import-questions-d1.mjs`（本地离线验证 969 题导入 ✅ + 导出线上 SQL）+ `db/import-questions.sql`（625KB，969 条 INSERT）+ `wrangler.toml` 加 `TRIUMPH_D1` 绑定（database_id 待 `wrangler d1 create` 后填入）
- **后台 SPA**：`admin/`（React 18 + Vite + TS + Tailwind，独立 npm 项目，构建到 `site/admin/`）；登录页（复用 /api/login + 服务端管理员校验）、路由守卫、侧边栏布局、Dashboard 概览 + 5 个占位页（用户/订单/工单/内容/分析）
- **worker 门禁**：`worker-src/admin.mjs`（`/api/admin/me`，ADMIN_EMAILS 环境变量白名单，大写邮箱归一化）+ `test/admin-gate.test.mjs`（5 项测试）

**⚠️ 上线前必须做（部署时）**：
- Cloudflare 控制台给 `triumph` 项目加环境变量 `ADMIN_EMAILS`（你的管理员邮箱，逗号分隔可多个）
- 创建 D1：`wrangler d1 create triumph_db` → 把 database_id 填进 `wrangler.toml` → `wrangler d1 execute triumph_db --file=db/schema.sql` → `wrangler d1 execute triumph_db --file=db/import-questions.sql`

**✅ 线上部署完成（2026-08-23）**：
- D1 已创建（id `5d04a307-4dc9-4236-9492-817f17f3a351`）并填进 `wrangler.toml`；schema 12 表已建；969 题已导入线上 D1（5002=214/5003=172/5004=240/5005=343）
- `ADMIN_EMAILS` 已在 Cloudflare 控制台配置（`18332957502@163.com`）
- 管理员账号已验证：`18332957502@163.com`（KV verified=true）
- 后台 `/admin` 已部署，**管理员登录测试通过 ✅**
- 诊断端点与调试代码已全部清理，KV 中 21 条 diag 垃圾数据已删除

**⚠️ 教训记录（本次会话踩坑）**：
- `wrangler kv key list` 默认分页只显示开头几条（`apikey:` 排前），查用户记录必须用 `--prefix="users:"` 或 API 直查，否则会误判"无数据"
- curl 从本机模拟请求会被 Cloudflare 边缘 AI/Bot 拦截返回 502（text/plain），不代表 worker 故障；用户浏览器能正常走通的功能优先信用户反馈
- D1 操作必须带 `--remote`（本地 D1 在 `.wrangler\state\v3\d1`，与线上隔离）；曾把 schema 导进本地库，线上 webhook 落库静默失败
- Creem REST API 字段是 snake_case（product_id/success_url），SDK 文档的 camelCase 不能直接用于 REST
- Creem **测试环境与生产环境完全隔离**：独立的 API key（creem_test_）、独立的 webhook secret、独立的产品

---

## 2026-08-23 深夜 · P1 收银台完成（测试模式全链路打通）

**做了什么**：
1. **Creem 收银台**（`worker-src/billing.mjs`）：
   - `POST /api/billing/checkout`：创建结账会话（test/live 环境自动切产品与 key）
   - `POST /api/billing/webhook`：HMAC-SHA256 验签 + webhook_events 幂等 + 订阅/订单落 D1 + KV 权益更新
   - `POST /api/billing/portal`：客户门户链接（`/customers/billing`）
2. **密码重置**（`worker-src/password.mjs`）：`/api/password/forgot`（防枚举）+ `/api/password/reset`（token 用后即焚）
3. **管理员代办重置**：`/api/admin/users/password-reset-link`（返回 30 分钟一次性链接）
4. **后台用户列表**：`/api/admin/users`（KV 分页 + 脱敏，不含 saltHex/hashHex）+ admin SPA Users 页真实化（表格 + 重置按钮 + 复制链接）
5. **权益展示**：`/api/me` 返回 `entitlements`；`upgrade.html`（升级页）+ dashboard PRO 徽章

**验证（全部通过）**：
- 测试 60/60 全绿（checkout 字段断言、webhook 验签/幂等/权益、密码重置端到端、用户列表权限/脱敏）
- **真实测试支付成功**：upgrade.html → Creem 测试结账页 → 测试卡 4111 1111 1111 1111 → 跳回显示 PRO ✅
- 账号 `18332957502@163.com` KV 权益：plan=pro, subscriptionStatus=trialing, creemCustomerId=cust_3u9q0N3ISXIuirEeun7og2
- D1 webhook_events 3 条处理成功；subscriptions 表有 sub_41VEiz1BXLYFkxF1Zrf7yG

**线上 Secrets 配置（Cloudflare 控制台 / wrangler）**：
- `CREEM_API_KEY`（生产 key，⚠️ 已在对话中暴露，**建议联调后轮换**）
- `CREEM_TEST_API_KEY`（测试 key，`creem_test_TXidJAJoZcO7KQ8i7osNi`）
- `CREEM_MODE=test`（**当前是测试模式**；审核通过后切 live）
- `CREEM_PRODUCT_ID`（生产产品 `prod_7EK5eK5RLfQZYK2DCzaetg`）
- `CREEM_TEST_PRODUCT_ID`（测试产品 `prod_6YuHXkbpSu7Lvyh7YMtoq9`）
- `CREEM_WEBHOOK_SECRET`（生产 webhook secret `whsec_2Qdf...`）
- `CREEM_TEST_WEBHOOK_SECRET`（测试 webhook secret `whsec_4Z5VJHwLVISfoAchDtfB9m`）

**遗留/下一步**：
- ⚠️ **Creem API key 轮换**：用户发过 key 在对话里，测试稳定后去 Creem 控制台重建，更新 `CREEM_API_KEY`
- ⚠️ **审核通过后切 live**：`CREEM_MODE` 改 live，确认生产 webhook endpoint 已配置
- P2：内容管理（题库后台 CRUD、文章编辑器、工具注册表）——规划文档 §7
- P3：行为分析（埋点 AE + 看板）、工单退款流——规划文档 §8
- "Renews on —"：测试环境 subscription 事件未带 current_period_end，生产环境正常后核对
- git 已提交：`8cfdeb4`（128 文件，P0+P1 全部成果）

---

## 2026-08-24 · 四州要求调研结果发布（PA/AL/MD 州页 + 资源清单同步）

**背景**：本会话完成 ETS 官方页面 + 州一手来源（KY 法规 16 KAR 6:010、ALSDE Manual B 2024、ETS 产品页）的 Praxis elementary 调研。结论：仅 Kentucky 仍把 5001 列为现行法定要求（157/157/155/159 + PLT 5622=160）；PA 小学用 PECT、MD 用 CKT 7811、AL 已弃 5001（5002 于 2020-09-01 取消，现行 5901 + Foundations of Reading 190 + edTPA）。调研文档：`E:\Triumph\praxis-elementary-4-states-research.md`。

**新页面（3 个，静态 HTML + md 源）**：
- `site/praxis-5001-pennsylvania-requirements.html` + `site/md/…md`（PECT/5154+5155）
- `site/praxis-5001-alabama-requirements.html` + `site/md/…md`（5901 数学157/社会154；科学线未确认，文中已注明以 ETS scores?sid=AL 为准）
- `site/praxis-5001-maryland-requirements.html` + `site/md/…md`（5205 + 7811 CKT 161/150/154/161）

**同步的文件**：
- `site/resources.html`（静态卡片 +3，共 20 篇；注意站点已从 React+Babel 壳改为静态预渲染）
- `site/md/resources.md`（列表 +3）
- `site/sitemap.xml`（+3 URL，lastmod 2026-08-24）
- `site/llms.txt`（State guides 节 +3 行）
- `worker-src/content.mjs`（MD_ROUTES +3，**需重新构建 worker 生效**：`npm run build:worker`，输出到 `site/_worker.js`）

**⚠️ 上线步骤（沙箱内无法代跑）**：`npm run build:worker` → `deploy.ps1` 部署 → 用 PageSpeed Insights 复查（resources 已静态化，React dev+Babel 移除）。文中"未确认"项（AL 5905/5005 科学线、FoR190 费用、PA 5155 分数线）发布前建议人工复核 ETS scores 页。

**其他观察**：resources.html 在 2026-08-25 00:24 被外部再生成（React 壳 → 静态），若后续再跑生成脚本，请确认生成器以 `site/md/resources.md` 为资源列表源，避免覆盖本次新增条目。
